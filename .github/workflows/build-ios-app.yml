
name: build-ios-app
on:
  push:
    branches:
      - 'main'
jobs:
  build:
    runs-on: macOS-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        token: <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>
      - name: Build app
        run: fastlane custom_lane
        env:
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}


